# Plan: Rewrite `d-cup-vs-dd-cup` as 34DD vs 36D Sister-Size Page

## Context

The user provided a 5-point technical SEO brief to refactor a single page
`d:\DevProject\breastcalculator\bra-size-guide\compare\d-cup-vs-dd-cup\index.html`
from a "D Cup vs DD Cup" generic cup-comparison into a deep "34DD vs 36D"
sister-size comparison.

**Why now:** The page currently has:
- Title: `D Cup vs DD Cup: Visual Comparison & Differences` (generic, no
  search-volume-anchored intent)
- H1: `D Cup vs DD Cup: Visual Comparison, Volume & Best Bras`
- Body compares the **entire D cup group vs DD cup group**, not a real
  sister-size band swap
- No cross-link to `/article/bra-sister-sizes-explained/`, no cross-link
  to single-band pages `/bra-size-guide/34dd/` or `/bra-size-guide/36d/`
- FAQ schema asks 6 generic "D vs DD" questions, none of the high-volume
  E-A-T sister-size queries ("Are 34DD and 36D the same?", "Is 34DD
  bigger than 36D?", "Should I switch from 36D to 34DD?")

**Critical pre-existing finding:** The site **already has** a high-quality
`34dd-vs-36d` sister-size page at
`d:\DevProject\breastcalculator\bra-size-guide\compare\34dd-vs-36d\index.html`.
The user explicitly chose to keep the URL `d-cup-vs-dd-cup` (it is
referenced from 38 files) and add a `<link rel="canonical">` pointing
to `34dd-vs-36d` to avoid keyword cannibalization. The result is a
**semantic marketing expansion page** that uses user-supplied content
(34DD vs 36D body, FAQ, internal links) and hands ranking authority to
the primary `34dd-vs-36d` page.

## Files to Modify

**Single file:** `d:\DevProject\breastcalculator\bra-size-guide\compare\d-cup-vs-dd-cup\index.html`

Out of scope (kept as-is):
- `sitemap.xml` (URL slug unchanged)
- 38 other pages that link to this URL
- `scripts/_compare-skeletons.js` and `_compare-skeletons2.js` only
  reference file paths, not title/H1 — confirmed safe
- `dist/bra-size-guide/compare/d-cup-vs-dd-cup/index.html` — will be
  regenerated by `npm run build` after the source change

## Implementation

### 1. `<head>` block — Title, meta, canonical, hreflang

In `<head>` lines 28-49 of the current file:
- `<title>` → `34DD vs 36D Bra Size: Volume, Fit Differences & Sister Sizes`
  (verbatim from user brief, line 28-30)
- `meta name="description"` → copy the high-intent pattern from
  `34dd-vs-36d` (line 31) but rewrite the hook to lead with volume + fit
  + sister-size language
- `meta name="keywords"` → add `34DD vs 36D, 34DD bra size, 36D bra size,
  sister size comparison, 34DD vs 36D sister size`
- **Update `<link rel="canonical" href="...">`** (line 34) to point at
  the primary page:
  `https://breastcalculator.com/bra-size-guide/compare/34dd-vs-36d/`
- **Keep `<link rel="alternate" hreflang="en" href="...">` and
  `hreflang="x-default"`** (lines 35-36) pointing to the **original URL**
  `https://breastcalculator.com/bra-size-guide/compare/d-cup-vs-dd-cup/`
  (per user brief: "Maintain the current canonical and hreflang setups")
  — but the canonical is the one that **points to 34dd-vs-36d** while
  hreflang stays at the original URL. This is intentional: hreflang is a
  "this URL is the en-version of itself" statement and stays local;
  canonical is a "credit the primary" statement and moves.

  **Wait — this needs to be resolved before writing.** A canonical
  pointing to a different URL combined with hreflang that *also* points
  to that different URL is the Google-recommended pattern for
  duplicate-with-merging. But user said "Maintain the current canonical
  and hreflang setups" which is ambiguous: does "current" mean before or
  after our changes? I will interpret this as: keep the **URL values**
  exactly as they are today. This means we add a *second* canonical
  signal — we set the canonical to the new sister-size page, but
  preserve the hreflang self-reference at the original URL. The two
  signals are not contradictory: canonical says "this content is
  duplicate of X" while hreflang says "this URL is the en-version of
  itself" — Google honors both.

  **Re-confirm with user before writing** — see Question block below.

- `og:title`, `og:description`, `twitter:title`, `twitter:description`
  → mirror the new `<title>` and `description`
- `og:url` (line 39) → keep pointing to the **original URL**
  `https://breastcalculator.com/bra-size-guide/compare/d-cup-vs-dd-cup/`
  (URL didn't change)
- `<script type="application/ld+json">` for Article (lines 411-433) →
  rewrite `headline` and `description` to match new topic, keep dates
  and author/publisher block
- `og:type` (line 45) stays `"website"` (compare page, not editorial)

### 2. JSON-LD schemas inside `<main>` (lines 411-515)

**FAQPage** (lines 434-489) — **REPLACE 6 generic D vs DD questions
with 3 user-supplied sister-size questions, exactly as user wrote**:

- Q1 `"Are 34DD and 36D the same size?"` → answer verbatim
- Q2 `"Is a 34DD bigger than a 36D?"` → answer verbatim
- Q3 `"How do I know if I should switch from 36D to 34DD?"` → answer verbatim

**Article** (lines 411-433) — update `headline` to
`"34DD vs 36D Bra Size: Volume, Fit Differences & Sister Sizes"`,
update `description` to reflect sister-size topic. Keep `datePublished:
2026-06-01` and bump `dateModified` to today.

**BreadcrumbList** (lines 490-515) — keep the 3 crumb structure, update
the third item:
- (1) Home → `https://breastcalculator.com/`
- (2) Bra Size Comparisons → `https://breastcalculator.com/bra-size-guide/compare/`
- (3) `D Cup vs DD Cup` → **`34DD vs 36D Sister Size`** with
  `item: https://breastcalculator.com/bra-size-guide/compare/d-cup-vs-dd-cup/`
  (use the original URL since hreflang/canonical still resolves here)

### 3. Hero (lines 516-523) — rewrite

```html
<section class="hero">
  <h1>34DD vs 36D Bra Size Comparison: Full Support Guide</h1>
  <p>Sister sizes with the same cup volume — which band fit gives you
     the support and comfort you need?</p>
</section>
```

### 4. Article body (lines 524-864) — full rewrite, follow user brief

Replace the entire `<article class="article" style="...">...</article>`
block with this structure. All text is original / user-supplied, no
paraphrasing of copyrighted material.

**H1 (line 517)**: `34DD vs 36D Bra Size Comparison: Full Support Guide`

**Intro paragraph (no H2 above it)**: A 60-90 word sister-size opener
that mentions "sister sizes" with an inline link to
`/article/bra-sister-sizes-explained/` using anchor text
"bra sister sizes". The link target is verified to exist.

**H2 #1**: `The Science of Sister Sizes: Volume vs. Band Fit`
- Paragraph explaining the "band up, cup down" rule
- Includes internal link to `/article/bra-sister-sizes-explained/`
  using anchor "bra sister sizes" again (the user said "Find the
  sentence mentioning sister sizes" — there are multiple; we add the
  link in the most natural sentence)
- Stat callouts `<strong>~700 cc</strong>` per breast and
  `<strong>700-900 grams</strong>` per breast

**H2 #2**: `34DD vs 36D Dimensions: The Detailed Breakdown`
- Placed immediately above the comparison table
- Wraps the new comparison table

**Comparison table** (replace existing D-vs-DD table):
- Wrap in `<div class="table-wrap">` (existing convention)
- Add `aria-label="Side-by-side comparison of 34DD and 36D bra sizes"`
  to the table (per user brief: explicit accessibility attribute)
- Columns: Feature | 34DD | 36D
- Rows: Band Size | Cup Volume | Bust Circumference | Sister Sizes
  | Best For
- Use `<strong>` to wrap numerical stats

**H2 #3**: `When to Wear a 34DD Bra`
- 60-80 words
- Cross-link to `/bra-size-guide/34dd/` using anchor
  "34DD bra size guide" (first mention) and "34DD bra" (second mention)

**H2 #4**: `When to Wear a 36D Bra`
- 60-80 words
- Cross-link to `/bra-size-guide/36d/` using anchor "36D bra size guide"

**H2 #5**: `Critical Fit Signs for D/DD+ Cups`
- 80-100 words intro

**H3 #1**: `How to Tell If Your 36D Band Is Too Loose`
- 50-70 words, fits under H2 #5

**H3 #2**: `How to Tell If Your 34DD Cups Are Too Small`
- 50-70 words, fits under H2 #5

**Closing block**: 40-60 words summary plus a `<strong>`-wrapped
"Not sure what your size is?" sentence with links to
`/bra-size-calculator/` and `/bra-size-guide/`.

### 5. CTA banner (lines 865-877) — keep unchanged

Already says "Not Sure Your Exact Size?" → links to bra-size-calculator.
Fits the new topic perfectly, no change needed.

### 6. related-links (lines 878-927) — minimal update

- Update "D Cup vs DD Cup" card in any title/anchor text where it
  references the old D-vs-DD topic. Verify only the topmost
  related-guides card needs a title swap from "D Cup vs DD Cup" to
  something neutral like "Cup Size Differences" or simply remove that
  card if it duplicates content. **Simpler: change the card title to
  "Visual Size Chart" if it isn't already.**
- Verify the "Explore More Comparisons" grid: keep
  `b-cup-vs-c-cup`, `c-cup-vs-d-cup`, `dd-cup-vs-ddd-cup`,
  `wireless-vs-wired-bra` as the 4 sister-comparisons. The
  `34dd-vs-36d` link will NOT be added here (the canonical already
  signals the relationship).

### 7. Verification

After the file is saved:

1. **Internal audit** — `python scripts/seo/hreflang_audit.py --no-http`
   must still report 0 issues (the URL and the en/x-default hreflang
   are unchanged; only the canonical now points elsewhere).
2. **FAQPage validation** — Open the saved file, copy the JSON-LD
   into Google's Rich Results Test
   (https://search.google.com/test/rich-results) to confirm FAQPage
   schema parses cleanly.
3. **Build sync** — `npm run build` to regenerate
   `dist/bra-size-guide/compare/d-cup-vs-dd-cup/index.html`.
4. **Dist verification** — confirm dist file contains:
   - New `<title>`
   - New canonical URL pointing to 34dd-vs-36d
   - 3 FAQPage questions (verbatim from user brief)
   - 3 internal links: 1 to `/article/bra-sister-sizes-explained/`,
     1+ to `/bra-size-guide/34dd/`, 1+ to `/bra-size-guide/36d/`
   - `<strong>` wrapping for `~700 cc` and `700-900 grams`
5. **Sitemap unchanged** — confirm `d-cup-vs-dd-cup/` is still listed
   in `dist/sitemap.xml` (it is — the sitemap only references URLs,
   not titles).

## Critical Decisions to Confirm with User

(1) **Canonical vs hreflang resolution.** User said "Maintain the
current canonical and hreflang setups" but also chose "add canonical
pointing to 34dd-vs-36d." I read this as: hreflang self-reference
stays at the original URL (because hreflang *is* the self-reference),
but the new canonical moves to 34dd-vs-36d. **Confirm this reading.**

(2) **FAQs being shrunk from 6 to 3.** The current page has 6
FAQPage entries; user spec provides 3. Replace or append? I will
**replace** because adding 3 more would create schema bloat and the
user spec is clear ("Update and expand the FAQPage schema to match
high-volume E-A-T search questions" — 3 questions, not 6+3).

(3) **The original D vs DD comparison content** (the D-cup vs DD-cup
intro, the table rows about "bust-underbust difference" etc.) is
being deleted entirely. The new page is purely sister-size-focused.
**Confirm OK to delete old D-vs-DD content.** Recovery: full backup
saved to `.backup/d-cup-vs-dd-cup/<timestamp>/index.html` before edit.

## Estimated Diff Size

- Current `<head>` block: ~22 lines, +6 lines (new title/desc/canon)
- Current `<main>` schemas: ~104 lines, mostly replace
- Current article body: ~340 lines, fully replaced with ~280 lines
- Net: roughly same line count, but every block 1:1 refactored
